tls_server_config:
  cert_file: "{{ thanos_tls_cert }}"
  key_file: "{{ thanos_tls_key }}"

basic_auth_users:
  {% for user, hash in thanos_basic_auth_users.items() %}
  {{ user }}: {{ hash }}
  {% endfor %}
